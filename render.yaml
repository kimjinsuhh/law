services:
  - type: web
    name: taxlaw-crawler
    env: python
    buildCommand: |
      pip install -r requirements.txt
      playwright install chromium
    startCommand: python main.py
    autoDeploy: false
